

const ProfessionalsProfile = () => {
    // axiosPublic.patch(`/camp/professionals/${camp._id}`,  { professionals: parseFloat(camp?.professionals) }) 
    // .then(res=> {console.log(res?.data)})
    return (
        <div>
            
        </div>
    );
};

export default ProfessionalsProfile;